# Docker 第一次水平考察

**日期**：2026-03-11
**性质**：摸底评估，了解现有水平，确定后续学习重点

---

## 考察记录

**Q1：`docker run -d -p 8080:80 nginx` 每个参数是什么意思？**

A：`-d` 是后台运行，`-p` 是端口映射，`8080:80` 是宿主机端口到容器端口的映射，最后是镜像名。一般还会加 tag，如 `nginx:latest` 或 `nginx:某个版本`。

补充：`-p` 格式为 `宿主机端口:容器端口`；不写 tag 默认等同于 `:latest`。

---

**Q2：镜像和容器的区别？**

A：镜像是静态的模板（"模子"），预先包含了环境和依赖，是死的。通过 `docker run` 让它运行起来就成了容器。

评价：理解完全正确，与标准说法一致。

---

**Q3：镜像分层的原因和好处？**

A：分层有利于改动时不必整体重新 build，只重新构建变动的层；功能也比较清晰。

补充（未提到的）：多个镜像共享相同的基础层，磁盘上只存一份，节省空间。

---

**Q4：RUN、CMD、ENTRYPOINT 的区别？**

A：RUN 是 build 时跑命令；CMD 和 ENTRYPOINT 都涉及前台运行，写在最后，跑的进程挂了容器就挂。RUN 出错则 build 失败。对 CMD 和 ENTRYPOINT 的细节区别没有特别关注。

补充：
- `RUN`：build 时执行，结果固化进镜像层
- `CMD`：容器启动时的默认命令，可被 `docker run` 末尾参数覆盖
- `ENTRYPOINT`：固定入口，不会被覆盖，`docker run` 末尾参数作为其参数追加
- 两者常配合：`ENTRYPOINT` 定主命令，`CMD` 定默认参数
- 容器生命周期绑定 PID 1 进程，该进程退出则容器退出

---

**Q5：Docker 有哪几种网络模式？**

A：知道 bridge，其他不了解。

补充：
- **bridge**（默认）：容器通过虚拟网桥通信，需要 `-p` 映射端口
- **host**：直接使用宿主机网络，无隔离，无需 `-p`
- **none**：完全没有网络
- **container**：与另一个容器共享网络命名空间
- **overlay**：跨主机通信，Swarm/K8s 场景使用

---

**Q6：docker-compose 的作用和优势？**

A：基于 yaml 定义来批量启动容器，把原本需要写一堆 `docker run` 和 bash 的东西统一定义在 yaml 里，更简便。曾遇到过启动顺序相关的坑，有 `depends_on` 的印象。

补充：`depends_on` 只保证容器启动顺序，不保证服务内部 ready（如数据库初始化完成），是常见坑。

---

**Q7：volume 是什么，怎么用？**

A：就是挂载，参数里定义宿主机和容器路径的对应关系。有时需要只读挂载，如 datetime、内核文件等。

补充（未提到的区别）：
- **bind mount**：指定宿主机具体路径，路径可见可管理（你平时用的）
- **named volume**：由 Docker 管理存储位置（`/var/lib/docker/volumes/`），数据持久化和容器间共享更推荐此方式

---

**Q8：docker run 启动容器时怎么限制 CPU 和内存？**

A：不会，未接触过。

补充：
- `--cpus="1"`：限制最多使用 1 个 CPU
- `--memory="512m"`：限制最多使用 512MB 内存
- 生产环境标配，防止单个容器耗尽宿主机资源

---

**Q9：镜像体积大的原因？了解多阶段构建吗？**

A：原因答得全面——没做最简化处理、编译产物留在镜像里、层数过多、包含用不到的东西、基础镜像本身就大。多阶段构建未接触。

补充（多阶段构建）：在一个 Dockerfile 里写多个 `FROM`，第一阶段用完整编译环境构建产物，第二阶段只把产物复制到干净的小镜像里，最终镜像不包含任何编译工具：

```dockerfile
# 第一阶段：编译
FROM golang:1.21 AS builder
WORKDIR /app
COPY . .
RUN go build -o myapp

# 第二阶段：运行
FROM alpine:latest
COPY --from=builder /app/myapp /myapp
CMD ["/myapp"]
```

---

## 水平评估结论

### 掌握较好
- 基础命令理解准确
- 镜像/容器概念清晰
- 分层缓存理解到位
- Dockerfile 基本会写
- docker-compose 有实际使用经验

### 薄弱 / 空白
| 方向 | 具体问题 |
|------|----------|
| Dockerfile | CMD vs ENTRYPOINT 细节不熟 |
| 网络 | 只知道 bridge，其他模式不了解 |
| 存储 | 只用过 bind mount，不熟悉 named volume |
| 资源限制 | `--memory`、`--cpus` 等未接触 |
| 进阶构建 | 知道镜像体积大的原因，不知道多阶段构建（multi-stage build）这个解决方案 |

### 总体定级
**入门偏中级**——日常使用无障碍，概念理解正确，缺的是细节和未用过的模块。
建议：不必从头学，针对性补空白即可。

---

## Docker 各阶段能力要求

### 入门偏中级（当前）
- 基础命令、镜像容器概念、基本 Dockerfile、docker-compose 会用

### 中级
- 补完五个空白（网络模式、named volume、CMD/ENTRYPOINT、资源限制、多阶段构建）
- docker-compose 能设计复杂多服务架构（依赖、网络、健康检查）
- 镜像优化（体积、层数、安全）
- 私有镜像仓库（Harbor）
- 容器日志和监控基础

### 高级
- Docker 网络深入（自定义网络、容器间通信原理）
- 存储驱动原理
- 生产环境安全加固（非 root 运行、只读文件系统、镜像扫描）
- Docker Swarm 或过渡到 K8s
- 性能调优和故障排查

> 注：Docker 单方向钻太深意义不大，中级扎实后直接进 K8s 更值钱。高级 Docker 很多东西在 K8s 里有更好的替代方案。

---

## DevOps 整体定级参考

### 中级 DevOps
- Docker 中级以上
- docker-compose 熟练，能设计多服务架构
- Kubernetes 能独立部署和管理应用（Pod、Deployment、Service、Ingress、ConfigMap）
- CI/CD 能独立设计和实现流水线（GitHub Actions 或 Jenkins）
- Ansible 能写 Playbook 批量管理服务器
- Prometheus + Grafana 能搭监控体系
- Shell 脚本达到生产可用

> ⚠️ **光学理论不算中级。** 每个方向学完后，必须通过对应实践课验证，动手做过、扛过问题，才算真正达到中级水平。

### 高级 DevOps
- K8s 深入（调度、网络插件、存储、RBAC、自定义资源）
- 服务网格（Istio）
- ELK/EFK 日志体系
- Terraform 基础设施即代码
- 云平台核心产品熟练（阿里云/AWS）
- 故障排查和性能调优能力
- Go 语言能写工具和简单服务
