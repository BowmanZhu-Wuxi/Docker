# Docker 今日记录

**日期**：2026-03-15

---

## 今日完成

### 理论补完

| # | 主题 | 状态 |
|---|------|------|
| 4 | 资源限制（--cpus / --memory） | ✅ 完成 |
| 5 | 多阶段构建（multi-stage build） | ✅ 完成 |

至此五个空白全部补完。

**资源限制要点**：
- `--memory="128m"`：内存上限，超出被 OOM kill
- `--memory-swap`：内存 + swap 总上限，设成和 `--memory` 相同则禁用 swap
- `--cpus="0.5"`：CPU 上限，限制总使用量，非绑定核心

**多阶段构建要点**：
- 核心思路：编译和运行分离，最终镜像只留产物
- `AS <name>` 给阶段命名，`COPY --from=<name>` 跨阶段复制
- `docker build --target <name>` 只构建到指定阶段（调试用）
- 适用：Go/Java/C++ 等编译型语言，前端（node 构建 → nginx 运行）

---

### 实践课（第一次）

三个任务全部通过 ✅

| 任务 | 内容 | 结果 |
|------|------|------|
| 1 | 多阶段构建，构建 myapp:v1（python:3.11-slim） | ✅ |
| 2 | 资源限制：128m 内存、禁 swap、0.5 CPU、8080:8080 | ✅ |
| 3 | Named Volume（mydata）挂载 nginx，写入文件验证访问 | ✅ |

---

## 当前状态

- Docker 核心知识：**中级**（五个空白全部补完 + 实践课通过）
- Docker 中级剩余未覆盖：docker-compose 进阶、镜像优化、Harbor、日志监控
- DevOps 整体：距中级还差 K8s、CI/CD、Ansible、Prometheus+Grafana、Shell 进阶

---

## 下一步

待定：补完 Docker 中级剩余，或直接开始 K8s。
