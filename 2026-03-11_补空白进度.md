# Docker 补空白进度

**日期**：2026-03-11
**基于**：第一次水平考察的五个空白

---

## 进度总览

| # | 主题 | 状态 |
|---|------|------|
| 1 | CMD vs ENTRYPOINT | ✅ 完成 |
| 2 | 网络模式 | ✅ 完成 |
| 3 | Named Volume vs Bind Mount | ✅ 完成 |
| 4 | 资源限制（--cpus / --memory） | ✅ 完成 |
| 5 | 多阶段构建（multi-stage build） | ✅ 完成 |

---

## 1. CMD vs ENTRYPOINT ✅

- `CMD`：容器启动时的默认命令，可被 `docker run` 末尾参数**整体替换**
- `ENTRYPOINT`：固定入口，不会被替换，`docker run` 末尾参数作为其**追加参数**
- 常见组合：`ENTRYPOINT` 定主命令，`CMD` 定默认参数

```dockerfile
ENTRYPOINT ["echo"]
CMD ["default message"]
```

```bash
docker run myimage              # 输出: default message
docker run myimage custom msg   # 输出: custom msg
```

---

## 2. 网络模式 ✅

| 模式 | 隔离性 | 需要 -p | 典型场景 |
|------|--------|---------|----------|
| bridge | 有 | 是 | 默认，日常使用 |
| host | 无 | 否 | 追求性能，端口直通 |
| none | 完全隔离 | — | 安全隔离 |
| container | 共享另一个容器网络 | — | sidecar 模式 |
| overlay | 跨主机 | — | Swarm/K8s（暂跳过） |

**container 模式补充**：
- 共享 IP、网卡、端口空间，但文件系统和进程仍隔离
- 两个容器不能监听同一端口，否则冲突
- sidecar 典型用法：主容器监听 8080，代理容器监听 9090，内部用 localhost:8080 互通

---

## 3. Named Volume vs Bind Mount ✅

### 基本区别

| | Bind Mount | Named Volume |
|---|---|---|
| 路径 | 自己指定 | Docker 管理（/var/lib/docker/volumes/） |
| 底层 | bind mount | 也是 bind mount，只是 Docker 帮你建目录 |
| 适合场景 | 配置文件、代码、日志 | 数据库数据目录 |

### 挂文件 vs 挂目录

- 挂单文件：宿主机文件必须提前存在，否则会被创建为目录
- 挂目录：不存在会自动创建

### inode 问题（重要坑）

- bind mount 挂的是 inode，不是路径名
- **挂单文件**：锁定的是文件的 inode。vim 保存时会 rename（新建文件 → 替换），原 inode 还在但内容是旧的，容器内看到旧内容
- **挂目录**：锁定的是目录的 inode。目录 inode 存的是"名字→inode 的映射表"，文件被替换后目录项更新，容器内能看到新文件，不受影响

### inode 乱了怎么办

- `echo >` 写文件：写的是新 inode，对容器内的旧挂载无效
- **重启容器**：Docker 重新解析路径，挂到当前新 inode，唯一有效办法
- **根本解法**：挂目录而不是挂单文件，彻底避免此问题

---

## 4. 资源限制 ✅

- `--memory="512m"`：内存上限，超出会被 OOM kill
- `--memory-swap="512m"`：内存 + swap 的总上限，设成和 `--memory` 相同则禁用 swap
- `--cpus="1"`：CPU 上限，可用小数（如 `0.5`），限制总使用量而非绑定到某个核

| 参数 | 作用 |
|------|------|
| `--memory="512m"` | 内存上限 512MB |
| `--memory-swap="512m"` | 禁用 swap |
| `--cpus="1"` | CPU 上限 1 核 |

---

## 5. 多阶段构建 ✅

核心思路：编译和运行分离，最终镜像只留产物，不含编译工具链。

```dockerfile
# 第一阶段：编译
FROM golang:1.21 AS builder
WORKDIR /app
COPY . .
RUN go build -o myapp

# 第二阶段：运行
FROM alpine:latest
COPY --from=builder /app/myapp /myapp
CMD ["/myapp"]
```

- `AS builder`：给阶段起名
- `COPY --from=builder`：从指定阶段复制文件
- `docker build --target builder`：只构建到某个阶段（调试用）
- 适用场景：Go/Java/C++ 等编译型语言，前端（node 构建 → nginx 运行）

---

## 全部完成 ✅

五个空白已全部补完，Docker 达到中级水平。
