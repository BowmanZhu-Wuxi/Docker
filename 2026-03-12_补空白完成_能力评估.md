# Docker 补空白完成 · 能力评估

**日期**：2026-03-12
**基于**：2026-03-11 第一次水平考察 + 今日补完五个空白

---

## 今日完成内容

| # | 主题 | 说明 |
|---|------|------|
| 4 | 资源限制（--cpus / --memory） | 从完全未接触 → 理解参数含义和底层逻辑 |
| 5 | 多阶段构建（multi-stage build） | 从未接触 → 理解核心思路和写法 |

（1~3 已于 2026-03-11 完成）

---

## 当前能力边界

### 掌握

- 基础命令：`docker run`、`docker ps`、`docker images` 等日常操作无障碍
- 镜像/容器概念清晰，分层缓存理解到位
- Dockerfile 能写：`RUN`、`CMD`、`ENTRYPOINT` 区别和组合用法掌握
- 网络模式：bridge / host / none / container / overlay 全部了解，container 模式的 sidecar 场景理解
- 存储：bind mount 和 named volume 区别清楚，inode 坑知道
- 资源限制：`--memory`、`--memory-swap`、`--cpus` 参数含义和用法
- 多阶段构建：核心思路（编译运行分离）、写法、`--target` 调试用法
- docker-compose：有实际使用经验，知道 `depends_on` 的坑

### 尚未覆盖（中级剩余）

- docker-compose 复杂多服务架构：网络配置、健康检查、完整依赖设计
- 镜像优化：层数控制、`.dockerignore`、distroless 等
- 私有镜像仓库（Harbor）
- 容器日志和监控基础（`docker stats`、`docker logs`）

---

## 定级

**中级（入门）**

五个空白全部补完，Docker 核心知识覆盖完整。
距离"中级扎实"还差 docker-compose 进阶和镜像优化实践，但知识体系已无明显盲区。

---

## 下一步建议

> Docker 单方向钻太深意义不大，中级扎实后直接进 K8s 更值钱。

两条路可选：

1. **补完 Docker 中级剩余** — docker-compose 进阶 + 镜像优化，把中级打扎实再走
2. **直接开始 K8s** — Docker 现有水平已够用作为 K8s 的基础

> ⚠️ **理论过了不等于中级。** 每个模块学完后，必须配合实践课动手验证。没有实践，知识不算真正掌握，DevOps 中级更无从谈起。实践课由 Claude 出题，做过、扛过问题，才算该模块真正完成。

---

## 实践课记录

**日期**：2026-03-12
**结果**：三个任务全部通过 ✅

| 任务 | 内容 | 结果 |
|------|------|------|
| 1 | 多阶段构建 — 构建 myapp:v1 | ✅ |
| 2 | 资源限制 + 端口映射 — 128m 内存、禁 swap、0.5 CPU | ✅ |
| 3 | Named Volume — nginx + mydata volume，写入文件验证访问 | ✅ |

详细记录见：`2026-03-12_实践课_第一次.md`

---
