# Docker 实践课 · 第一次

**日期**：2026-03-12
**性质**：理论补完后的动手验证，考察多阶段构建、资源限制、Named Volume

---

## 任务一：多阶段构建

**要求**：写一个两阶段 Dockerfile，基于 Python 应用，第二阶段用 `python:3.11-slim`，tag 为 `myapp:v1`。

**实际做法**：

```dockerfile
FROM python:3.11.15-alpine3.23 AS builderbowman
COPY ./ /tmp/
RUN echo "没啥事做。"

FROM python:3.11-slim
COPY --from=builderbowman /tmp/app.py /tmpbowman/
CMD ["python","/tmpbowman/app.py"]
```

**结果**：✅ 构建成功，镜像 45.4MB，结构正确，`COPY --from=` 用法准确。

**备注**：第一阶段用的是 alpine 变体，自己注释了"没啥事做"——说明清楚知道 Python 无编译步骤，第一阶段形式大于内容，认知正确。

---

## 任务二：资源限制 + 端口映射

**要求**：启动 myapp:v1，后台运行，8080:8080，内存 128m，禁用 swap，CPU 0.5。

**实际结果**：

| 参数 | 要求 | 实测值 |
|------|------|--------|
| Memory | 128MB | 134217728 bytes = 128MB ✅ |
| MemorySwap | 同 memory（禁 swap） | 134217728 bytes ✅ |
| NanoCpus | 0.5 CPU | 500000000 ✅ |
| 端口 | 8080:8080 | ✅ |

**结果**：✅ 全部正确。

---

## 任务三：Named Volume

**要求**：启动 nginx:alpine，创建 named volume `mydata` 挂载到 `/usr/share/nginx/html`，宿主机端口映射容器 80，写入自定义文件后验证 nginx 能访问。

**实际做法**：
- 端口用 8089（8081 为生产服务占用，合理替换）
- 写入文件：
  ```bash
  sudo bash -c 'echo "This is a testbowman html page." > /var/lib/docker/volumes/mydata/_data/testbowman.html'
  ```
- 验证：
  ```bash
  curl localhost:8089/testbowman.html
  # 输出：This is a testbowman html page.
  ```

**结果**：✅ volume 挂载正确，写入文件后 nginx 成功访问。

---

## 总体评定

三个任务全部通过，Docker 核心知识（理论 + 实践）达到中级水平。
